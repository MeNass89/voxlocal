{
  "name": "dsh-profile-scribe",
  "private": true,
  "description": "VoxLocal clinical scribe: a dsh profile over the shipped web stack with the voxlocal-gpu provider",
  "engines": {
    "node": ">=22.19"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.7-rc.2"
  },
  "dsh": {
    "profile": {
      "bundles": [
        "@deepseek-ai/dsh-base",
        "@deepseek-ai/dsh-web-app"
      ]
    }
  }
}
